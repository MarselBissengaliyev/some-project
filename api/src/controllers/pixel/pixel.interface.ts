export interface CreatePixelBody {
  fb_pixel_id: string;
  token: string;
}

export interface UpdatePixelParams {
  pixelId: string;
}

export interface UpdatePixelBody {
  fb_pixel_id: string;
  token: string;
}
