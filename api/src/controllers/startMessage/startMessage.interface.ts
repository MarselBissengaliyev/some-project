export interface CreateStartMessageBody {
  message: string;
  photo: string;
  disable_web_page_preview: boolean;
}

export interface UpdateStartMessageBody {
  message: string;
  photo: string;
  disable_web_page_preview: boolean;
}